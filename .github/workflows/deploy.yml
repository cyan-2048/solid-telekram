name: deploy-preview
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  deploy-preview:
    name: deploy-preview
    runs-on: ubuntu-latest
    steps:
      - uses: oven-sh/setup-bun@v2
      # run any `bun` or `bunx` command
      - run: bun install
      - run: bun run deploy:preview